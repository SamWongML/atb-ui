{
  "name": "atb-console-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK (TypeScript) for the ATB console — ECS Fargate · ALB · ECR",
  "bin": {
    "atb": "bin/atb.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "cdk": "cdk"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "aws-cdk": "^2.1129.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
