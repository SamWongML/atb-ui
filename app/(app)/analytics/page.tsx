import { RoutePlaceholder } from "@/components/route-placeholder";

export default function AnalyticsPage() {
  return (
    <RoutePlaceholder
      title="Analytics"
      description="Cost and usage over time — Recharts. (Phase 4)"
    />
  );
}
